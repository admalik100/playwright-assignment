{
  "name": "playwright-assignment",
  "version": "1.0.0",
  "description": "Playing around with playwright to brush up skills",
  "scripts": {
    "playwright:test": "npx playwright test",
    "playwright:test:ui": "npx playwright test --ui",
    "playwright:test:chromium:headed": "npx playwright test --project=chromium --headed",
    "playwright:test:chromium": "npx playwright test --project=chromium",
    "playwright:test:debug": "npx playwright test --debug",
    "playwright:codegen": "npx playwright codegen",
    "playwright:report": "npx playwright show-report"
  },

  "author": "Ahmad Malik",
  "license": "ISC",
  "dependencies": {
    "@babel/eslint-parser": "^7.22.5",
    "dotenv": "^16.0.3",
    "eslint-plugin-playwright": "^0.12.0",
    "lint-staged": "^13.2.2",
    "prettier": "^2.8.8"
  },
  "devDependencies": {
    "@playwright/test": "^1.34.3"
    }
}
